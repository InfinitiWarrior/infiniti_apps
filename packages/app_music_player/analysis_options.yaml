include: ../../analysis_options.yaml

analyzer:
  # Vendored/patched third-party packages (see pubspec.yaml
  # dependency_overrides) — not ours to hold to this repo's lint rules.
  exclude:
    - third_party/**

# app_calculator

Scientific calculator with local calculation history. No network access, no permissions required.

Part of the [Infiniti Apps](../../README.md) monorepo — see the root `CLAUDE.md` for shared conventions.

## Run

```bash
flutter run -d linux   # fast iteration
flutter run             # connected Android device
```
